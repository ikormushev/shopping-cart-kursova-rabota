🛠 Инсталация и стартиране
Клонирайте репозиторито:


🛒 Smart Shopping Cart Service
Това е специализиран микросервис за управление на споделени пазарски колички, разработен като курсов проект. Системата предлага не само стандартни CRUD операции, но и интелигентни функции за оптимизация на разходите и колаборация в реално време.

Docker:
 -prerequisites : Execute the docker-compose.yaml in the shopping-cart-opps repo
 - Run the following command if you wish to execute flyway migrations to the database and run the app locally for debug
   * docker compose -f docker-compose.yml run --rm flyway-migrator
After executing migrations successfully you can run the application 
   * !! NOTE: some Windows users experience issues, due to docker reservations for Postgres port,
   try changing in the application.yml url: jdbc:postgresql://localhost:5432/shopping-cart?currentSchema=shopping_cart to port 6432
   * instead 5432



🚀 Основни функционалности
Shared Shopping Baskets: Потребителите могат да създават колички и да добавят членове (Collaborators) чрез уникален код за споделяне.

Batch Operations: Оптимизирано добавяне на множество продукти едновременно (Batch Insert) за намаляване на мрежовия трафик и подобряване на производителността.

Smart Price Comparison: Системата автоматично анализира продуктите в количката и предлага по-евтини алтернативи от други магазини в реално време.

Total Potential Savings: Изчисляване на текуща сума и "потенциална сума", ако потребителят приеме всички предложения за по-ниска цена.

Secure Session Management: Хибридна система за сесии (In-memory кеш + DB recovery), която поддържа активните колички на потребителите дори след рестарт.

Order History & Snapshots: При Checkout системата архивира поръчката и прави пълен snapshot на състоянието на продуктите (цени, количества, магазини) към момента на поръчката.

Caching for better perfomance and session management

Database migration tool - flyway-versioning

Git actions - създаване на снапшот/релийз имиджи, с цел разработка на отделни среди и релийз версии 

🏗 Технически стек
Backend: Java 21 (Spring Boot 3.5.0)

Database: PostgreSQL

ORM / Mapping: MyBatis (с анотации и вложени заявки за N+1 оптимизация)

Security: Spring Security с TextEncryptor за чувствителни данни

Caching: Session-based cache repository

Build Tool: Maven

📐 Архитектура
Проектът следва стриктна многослойна архитектура, която осигурява разделение на отговорностите:

Controller Layer: REST ендпоинти.


Facade Layer: Оркестрира логиката между различните услуги (UserService, BasketService, SessionManager) и подготвя обогатени DTO обекти за фронтенда.

Service Layer: Капсулира бизнес логиката.

Mapper Layer (MyBatis): Директна комуникация с базата данни чрез оптимизирани SQL заявки.

🔑 Важни API Ендпоинти
User Management
POST /api/user/register - Регистрация.

POST /api/user/login - Логин и генериране на сесия.

POST /api/user/change-password - Смяна на парола с проверка на старата.

POST /api/user/change-username - Смяна на име с имейл и парола.

Shopping Cart
POST /api/basket/add-batch - Добавяне на списък с продукти (Batch insert).

GET /api/basket/detailed - Пълна информация за количката, членовете и по-евтините предложения.

POST /api/history/checkout - Финализиране на поръчката и архивиране.
